<h1>backpacks</h1>
<div class="row">
    {{#if products.length}}
    {{#each products}}
    <div class="col s4">
        <div class="card">
            <div class="card-image waves-effect waves-block waves-light">
                <img class="activator" src="{{img}}">
            </div>
            <div class="card-content">
                <span>{{typeOfclothing}}</span>
                <span class="card-title activator grey-text text-darken-4">{{title}}</span>
                <p><a href="/backpacks/{{id}}">{{title}}</a></p>
            </div>
        </div>
    </div>
    {{/each}}
    {{else}}
    <p>В данной категории товаров отсутствуют.</p>
    {{/if}}
</div>